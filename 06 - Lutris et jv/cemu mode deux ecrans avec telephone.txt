sudo nala install  -y dkms git build-essential linux-headers-$(uname -r)


https://www.synaptics.com/products/displaylink-graphics/downloads/ubuntu


wget https://www.synaptics.com/sites/default/files/Ubuntu/pool/stable/main/all/synaptics-repository-keyring.deb
sudo nala install -y synaptics-repository-keyring.deb
rm synaptics-repository-keyring.deb
sudo nala update
sudo nala install -y displaylink-driver

sudo reboot now

et maintenant
xrandr --listproviders

sudo nala install -y evdi-dkms

modprobe evdi initial_device_count=2
echo "options evdi initial_device_count=2" | sudo tee -a /etc/modprobe.d/local-evdi.conf

xrandr --listproviders

